{
  "name": "duckdb-bun-shim",
  "version": "2.0.0-alpha39998",
  "description": "DuckDB 2 alpha release: use DuckDB 2 as the engine behind Bun's native bun:sqlite / Bun.SQL via a static SQLite-ABI shim.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/abelcha/bun-sqlite-duckdb-shim.git"
  },
  "duckdb": {
    "version": "v2.0.0-alpha39998",
    "source": "duckdb/duckdb",
    "commit": "9c21294d984dfe9a1da5d055b5fce3e8a0d634b2"
  },
  "main": "index.cjs",
  "types": "index.d.ts",
  "files": [
    "index.cjs",
    "index.d.ts",
    "prebuilt/",
    "README.md"
  ],
  "scripts": {
    "build:static": "bash scripts/build-static.sh",
    "test": "bun test ./test/nested.ts ./test/params-tx.ts ./test/v2-compat.ts",
    "test:legacy": "bun test ./test/nested.ts ./test/params-tx.ts",
    "test:v2": "bun test ./test/v2-compat.ts",
    "prepublishOnly": "node -e \"const f=require('node:fs'),p='darwin-arm64.dylib darwin-x64.dylib linux-x64.so linux-arm64.so'.split(' ');for(const b of p){if(!f.existsSync('prebuilt/'+b)){console.error('MISSING prebuilt/'+b+' — run CI build first');process.exit(1)}}console.log('core prebuilt binaries present')\""
  }
}
